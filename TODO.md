EXTRA TELEOP TODO:
1. No Arduino or CAN connected mode (test mode, output values but don't actually send them anywhere)
2. Manual control of all system components instead of state machine (really fucking annoying, probably a whole new controller)